# Data base credentials - {{ web_app_name }}
DATABASES_NAME="{{ db_name }}"
DATABASES_USER="{{ db_user_name }}"
DATABASES_PASSWORD="{{ db_password }}"
DATABASES_HOST="localhost"
DATABASES_PORT="5432"
